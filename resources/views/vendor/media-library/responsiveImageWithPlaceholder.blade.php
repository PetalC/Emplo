<img{!! $attributeString !!} @if($loadingAttributeValue) loading="{{ $loadingAttributeValue }}"@endif srcset="{{ $media->getSrcset($conversion) }}" onload="window.requestAnimationFrame(function(){if(!(size=getBoundingClientRect().width))return;onload=null;sizes=Math.ceil(size/window.innerWidth*100)+'vw';});" sizes="1px" src="{{ env( 'APP_ENV' ) === 'local' ? $media->getUrl($conversion) : $media->getTemporaryUrl( \Carbon\Carbon::now()->addMinutes( 5 ) ) }}" width="{{ $width }}" height="{{ $height }}">
